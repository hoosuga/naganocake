<h4>ジャンル編集</h4>

<%= form_with model: @genre, url: admin_genres_update_path, method: :patch do |f| %>
<div class="form-group">
  ジャンル名
  <%= f.text_field :name %>

  <%= button_tag(type: "submit", class: "btn btn-success") do %>
  変更を保存<% end %>
</div>
<% end %>